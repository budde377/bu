sudo: required
language: node_js
node_js:
  - 9
services:
  - docker

before_install:
  - npm i -g lerna

install:
  - lerna bootstrap

script:
  - lerna run test

deploy:
  - provider: script
    script: lerna run build:docker
    on:
      branch: ./scripts/deploy.sh
