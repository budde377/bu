{
	"devDependencies": {
		"lerna": "^2.9.1"
	},
	"scripts": {
		"ci": "lerna exec -- npm ci",
		"test": "lerna run test",
		"build:docker": "lerna run build:docker",
		"publish:docker": "lerna run publish:docker"
	}
}
